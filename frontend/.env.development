# CRA reads this in development. npm scripts also set PORT=3100 via cross-env.
PORT=3100
# If "Continue with Google" does not reach the API, set the backend origin (no trailing slash):
# REACT_APP_API_BASE_URL=http://127.0.0.1:10200
